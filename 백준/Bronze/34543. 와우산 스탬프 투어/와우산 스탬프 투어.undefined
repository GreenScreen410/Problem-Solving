#include <iostream>
using namespace std;

int main() {
  int N;
  cin >> N;

  int W;
  cin >> W;

  int score = N * 10;
  if (N >= 3) score += 20;
  if (N == 5) score += 50;
  if (W > 1000) score -= 15;

  cout << max(0, score) << "\n";

  return 0;
}