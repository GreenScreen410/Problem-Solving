#include <bits/stdc++.h>
using namespace std;

int main() {
  int N;
  cin >> N;
  
  unordered_map<string, int> m;
  for (int i = 0; i < N; i++) {
    string ingredient;
    cin >> ingredient;
    m[ingredient]++;
  }
  
  for (int i = 0; i < N - 1; i++) {
    string ingredient;
    cin >> ingredient;
    m[ingredient]--;
  }
  
  for (auto& [ingredient, x] : m) {
    if (x == 1) cout << ingredient << "\n";
  }
  
  return 0;
}