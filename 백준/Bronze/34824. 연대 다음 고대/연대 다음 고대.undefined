#include <bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);

  int n;
  cin >> n;

  while (n--) {
    string name;
    cin >> name;

    if (name == "yonsei") {
      cout << "Yonsei Won!\n";
      return 0;
    }
    if (name == "korea") {
      cout << "Yonsei Lost...\n";
      return 0;
    }
  }

  return 0;
}