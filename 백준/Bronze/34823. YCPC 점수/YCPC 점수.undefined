#include <bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);

  int y, c, p;
  cin >> y >> c >> p;

  cout << min({y, c / 2, p}) << "\n";

  return 0;
}