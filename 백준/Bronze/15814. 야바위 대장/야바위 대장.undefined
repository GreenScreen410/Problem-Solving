#include <iostream>
using namespace std;

int main() {
  string S;
  cin >> S;
  
  int T;
  cin >> T;
  
  for (int i = 0; i < T; i++) {
    int A, B;
    cin >> A >> B;
    swap(S[A], S[B]);
  }
  
  cout << S;
  
  return 0;
}