#include <bits/stdc++.h>
using namespace std;

int main() {
  int N;
  cin >> N;

  vector<int> a(N);
  for (int& i : a) cin >> i;
  ranges::sort(a);

  for (int& i : a) cout << i << " ";
  cout << "\n";

  return 0;
}